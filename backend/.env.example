# Copy to .env and fill in. For local dev you can also just export these in the shell.
#
# When your org BLOCKS service account key creation, leave GOOGLE_APPLICATION_CREDENTIALS
# unset and use: gcloud auth application-default login
#
# When you have a key file, use an ABSOLUTE path for GOOGLE_APPLICATION_CREDENTIALS.

GOOGLE_CLOUD_PROJECT=project-8be520bb-b4ac-4d57-a37
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/MedMate/backend/service-account.json
